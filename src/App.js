import './App.css';
import Game from "./pages/Game";

export default function App() {
    console.log(0 && 'The title')
  return (
    <div>
        <h1>{[].length && 'The title'}</h1>

        <Game />
    </div>
  );
}

